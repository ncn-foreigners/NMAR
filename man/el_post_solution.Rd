% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/engines_el_impl_core_helpers.R
\name{el_post_solution}
\alias{el_post_solution}
\title{Post-solution: denominators, masses, and mean (EL)}
\usage{
el_post_solution(
  estimates,
  missingness_model_matrix_scaled,
  missingness_model_matrix_unscaled,
  auxiliary_matrix_scaled,
  mu_x_scaled,
  response_outcome,
  family,
  N_pop,
  respondent_weights,
  K_beta,
  K_aux,
  nmar_scaling_recipe,
  standardize,
  trim_cap,
  X_centered = NULL
)
}
\arguments{
\item{estimates}{Numeric vector (beta, z, lambda) at the solution.}

\item{missingness_model_matrix_scaled}{Scaled design matrix for the missingness (response) model.}

\item{missingness_model_matrix_unscaled}{Unscaled design matrix for the missingness (response) model.}

\item{auxiliary_matrix_scaled}{Scaled auxiliary matrix (or empty matrix).}

\item{mu_x_scaled}{Vector of population means for scaled auxiliaries (or NULL).}

\item{response_outcome}{Numeric vector of respondent outcomes.}

\item{family}{Family object with linkinv and mu.eta.}

\item{N_pop}{Numeric; population total on the analysis scale.}

\item{respondent_weights}{Base weights for respondents.}

\item{K_beta, K_aux}{Integers; sizes of beta and lambda.}

\item{nmar_scaling_recipe}{Scaling recipe object for unscaling.}

\item{standardize}{Logical; whether coefficients need unscaling.}

\item{trim_cap}{Numeric; weight trimming cap (Inf = no trimming).}
}
\description{
Computes EL denominators and masses and the mean estimate after the solver
converges. The mean is calculated from normalized probability masses per
Qin, Leung, and Shao (2002, Eq. 11). When trimming is active, the masses are
capped and renormalized before computing the mean; diagnostics still report
constraint sums based on untrimmed masses. Denominator floors and the
guarding policy used in equations/Jacobian are applied consistently here for
diagnostic coherence.
}
\keyword{internal}
