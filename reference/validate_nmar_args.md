# Validate parsed NMAR inputs

Validate parsed NMAR inputs

## Usage

``` r
validate_nmar_args(spec, traits = list())
```

## Arguments

- spec:

  Object produced by \[parse_nmar_spec()\].

- traits:

  List of engine traits produced by \[engine_traits()\].
