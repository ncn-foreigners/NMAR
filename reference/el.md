# Empirical likelihood estimator

Empirical likelihood estimator

## Usage

``` r
el(data, ...)
```

## Arguments

- data:

  A `data.frame` or a `survey.design`.

- ...:

  Passed to class-specific methods.
