# Create a normalized NMAR task object

Create a normalized NMAR task object

## Usage

``` r
new_nmar_task(spec, traits)
```

## Arguments

- spec:

  Parsed NMAR specification produced by \[parse_nmar_spec()\].

- traits:

  Engine traits returned by \[engine_traits()\].
