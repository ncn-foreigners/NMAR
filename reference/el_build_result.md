# Build EL result object (success or failure)

Build EL result object (success or failure)

## Usage

``` r
el_build_result(
  core_results,
  input_spec,
  call,
  formula,
  engine_name = "empirical_likelihood"
)
```
