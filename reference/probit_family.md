# Construct a probit response family bundle

Construct a probit response family bundle

## Usage

``` r
probit_family()
```

## Value

A list with components `name`, `linkinv`, `mu.eta`, `d2mu.deta2`, and
`score_eta`.
