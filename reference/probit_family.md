# Construct probit response family

Construct probit response family

## Usage

``` r
probit_family()
```

## Value

A list with components `name`, `linkinv`, `mu.eta`, `d2mu.deta2`, and
`score_eta`.
