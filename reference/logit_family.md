# Construct a logit response family bundle

Construct a logit response family bundle

## Usage

``` r
logit_family()
```

## Value

A list with components `name`, `linkinv`, `mu.eta`, `d2mu.deta2`, and
`score_eta`.
