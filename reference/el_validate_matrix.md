# Validate matrix columns for NA and zero variance

Validate matrix columns for NA and zero variance

## Usage

``` r
el_validate_matrix(
  mat,
  allow_na,
  label,
  severity,
  row_map = NULL,
  scope_note = NULL,
  plural_label = FALSE
)
```
