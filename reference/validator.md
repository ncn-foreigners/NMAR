# Custom Validation Module

This module provides a set of internal helper functions for argument
validation. The functions are designed to throw a clear error message if
a validation check fails.

## Usage

``` r
validator
```

## Format

An object of class `environment` of length 13.
